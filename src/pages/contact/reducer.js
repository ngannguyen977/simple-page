const initialState = {
    state: []
}
export default function ContactReducer(state = initialState, action) {
    //  console.log('_______________Contact REDUCER :ACTION DATA___________', action)
    switch (action.type) {

        default: return state
    }
}
const initialState = {
    // state: []
}
export default function HomeReducer(state = initialState, action) {
    //   console.log('______________HOME REDUCER :ACTION DATA___________', action)
    switch (action.type) {

        default: return state
    }
}